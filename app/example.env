NEXT_PUBLIC_BACKEND_BASE_URL=http://localhost:8080

# Optional — pins the dashboard to one helmet instead of auto-selecting
# the first online one from GET /v1/helmets. Leave unset in normal
# multi-helmet operation.
# NEXT_PUBLIC_DEFAULT_HELMET_ID=HLM-0001
