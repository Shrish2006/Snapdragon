[project]
name = "snapdragon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ultralytics",
    "opencv-python",
    "huggingface-hub>=1.23.0",
]
