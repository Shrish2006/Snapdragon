apiVersion: v1
kind: Service
metadata:
  name: fall-detection
  namespace: safeguard
spec:
  selector:
    app: fall-detection
  type: NodePort
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30801
