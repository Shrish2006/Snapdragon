apiVersion: v1
kind: Service
metadata:
  name: ppe-detection
  namespace: safeguard
  labels:
    app.kubernetes.io/name: ppe-detection
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ppe-detection
  ports:
    - name: http
      port: 8000
      targetPort: 8000
