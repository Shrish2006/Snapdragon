[project]
name = "fall-detection"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]",
    "numpy",
    "matplotlib",
    "scikit-learn",
    "onnxruntime",
]
